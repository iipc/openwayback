<?xml version="1.0"?>
<project 
    xmlns:j="jelly:core" 
    xmlns:define="jelly:define" 
    xmlns:doc="doc" 
    xmlns:artifact="artifact" 
    xmlns:util="jelly:util" 
    xmlns:maven="jelly:maven"
    xmlns:ant="jelly:ant">

    <preGoal name="xdoc:jelly-transform">
        <attainGoal name="faq"/>
    </preGoal> 
    <postGoal name="dist:prepare-bin-filesystem">
        <!--Add the lib dir to our binary distribution.-->
        <mkdir dir="${maven.dist.bin.assembly.dir}/lib" />
        <copy todir="${maven.dist.bin.assembly.dir}/lib">
          <fileset dir="./lib">
            <include name="*.jar"/>
          </fileset>
        </copy>
        <attainGoal name="war" />
        <!--Copy over the just made war files into the distribution copy.-->
        <mkdir dir="${maven.dist.bin.assembly.dir}/webapp" />
        <copy todir="${maven.dist.bin.assembly.dir}/webapp">
          <fileset dir="${maven.build.dir}">
            <include name="*.war"/>
          </fileset>
        </copy>


        <move todir="${maven.dist.bin.assembly.dir}/lib">
          <fileset dir="${maven.dist.bin.assembly.dir}">
            <include name="wayback*.jar"/>
          </fileset>
        </move>

        <!--Copy to a bin dir the command line tools.-->
        <copy todir="${maven.dist.bin.assembly.dir}/bin">
          <fileset dir="${maven.src.dir}/scripts">
            <include name="bin-search"/>
            <include name="bdb-client"/>
            <include name="index-client"/>
            <include name="location-client"/>
            <include name="url-client"/>
            <include name="create-test-arc"/>
          </fileset>
        </copy>

    </postGoal>
</project>
